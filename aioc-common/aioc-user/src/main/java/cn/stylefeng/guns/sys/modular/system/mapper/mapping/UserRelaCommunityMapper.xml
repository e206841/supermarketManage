<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.stylefeng.guns.sys.modular.system.mapper.UserRelaCommunityMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.stylefeng.guns.sys.modular.system.entity.UserRelaCommunity">
        <id column="id" property="id" />
        <result column="userId" property="userId" />
        <result column="communityCode" property="communityCode" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, userId, communityCode
    </sql>
    <select id="deleteByUserId" parameterType="java.lang.Long">
        delete from user_rela_community where userId=#{userId}
    </select>
    <insert id="insertUserReal" parameterType="cn.stylefeng.guns.sys.modular.system.entity.UserRelaCommunity">
        INSERT INTO user_rela_community  ( userId, communityCode )  VALUES  ( #{userId}, #{communityCode} )
    </insert>

    <select id="getByUserId" parameterType="java.lang.Long" resultType="java.lang.String">
        select ur.communityCode AS code from user_rela_community ur
        where userId=#{userId}
    </select>
</mapper>
