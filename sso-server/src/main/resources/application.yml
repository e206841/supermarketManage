server:
  port: 8516
  servlet:
    context-path: /sso
#guns:
#  swagger-open: true              #是否开启swagger (true/false)
#  kaptcha-open: false             #是否开启登录时验证码 (true/false)
#  #  file-upload-path: d:/tmp       #文件上传目录(不配置的话为java.io.tmpdir目录)
#  spring-session-open: false      #是否开启spring session,如果是多机环境需要开启(true/false)
#  session-invalidate-time: 1800     #session失效时间(只在单机环境下生效，多机环境在SpringSessionConfig类中配置) 单位：秒
#  session-validation-interval: 900  #多久检测一次失效的session(只在单机环境下生效) 单位：秒
spring:
  application:
    name: guns-sso-server
  profiles:
    active: @spring.active@
  mvc:
#    static-path-pattern: /assets/**
    view:
      prefix: /pages
  datasource:
    url: jdbc:mysql://mysql-cn-north-1-4fda8775bfcc4f32.public.jcloud.com:3306/tianjin_aioc_new?autoReconnect=true&useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=CONVERT_TO_NULL&useSSL=false&serverTimezone=UTC
    username: aioc
    password: i8NnFvh6t
  aop:
    auto: true
    proxy-target-class: true
  redis:
    database: 0
    host: 116.196.88.6
    port: 6379
    pool:
      max-active: 10
      max-wait: -1
      max-idle: 2
    timeout: 5000
    password: ABCdef123
  jta:
    log-dir: ./ssoserver-log

mybatis-plus:
  typeAliasesPackage: com.stylefeng.sso.server.modular.entity

cors:
  origins: http://localhost:8080,http://127.0.0.1:8080,http://inspection.local.jdcloud.com:8080

log:
  path: sso-server